Min Occrences in getGloba... is set to 0, for larger datasets should be bigger (was: 5)

Added line "if len(coc[word].keys()) > 5:" (181) to ensure that no empty clusters exist

Turned off pruneVocabulary for single words tests

Gave newCorpus a list of words to disambiguate.